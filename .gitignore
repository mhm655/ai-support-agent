# Local-only file, not part of the project — a generated full-source-code
# dump of unclear origin. Kept locally but never tracked.
/project.md

# Local Claude Code config -- kept on disk, not pushed to the public repo
.claude/
CLAUDE.md
frontend/CLAUDE.md

# Vercel CLI link, and the OIDC token it writes into a local .env.local.
# Deliberately not a bare `.env*`: that pattern would also ignore the
# tracked .env.example files, which are meant to be in the repo.
.vercel
.env*.local
