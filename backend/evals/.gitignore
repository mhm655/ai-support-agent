# Cached Gemini embeddings. Regenerated on demand, ~19MB, and purely a
# speed/quota optimisation - see common.py.
.embedding_cache/

# Points at a specific agent row in whichever Supabase project .env
# targets, so it is environment-specific rather than shared.
.eval_agent_id

# Regenerated by make_test_pdf.py.
fixtures/

# Raw per-run locust CSVs. The distilled ramp results live in
# results/loadtest_l1.json, which is what LOADTEST.md cites.
results/loadtest/
